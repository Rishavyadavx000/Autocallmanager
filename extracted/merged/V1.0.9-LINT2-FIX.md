# V1.0.9 Lint #2 Fix

GitHub Actions reported:

`Diagnostics.kt:45: Missing permissions required by ConnectivityManager.getActiveNetwork:
android.permission.ACCESS_NETWORK_STATE`

Fix:
- Added `android.permission.ACCESS_NETWORK_STATE` to the application manifest.
- No lint suppression or baseline was added.
- No call scheduling/call execution logic was changed.

The permission is a normal Android permission and does not require a runtime
permission dialog.
