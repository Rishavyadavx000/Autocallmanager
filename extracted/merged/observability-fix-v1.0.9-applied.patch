diff --git a/app/src/main/java/com/example/autocallmanager/CallManager.kt b/app/src/main/java/com/example/autocallmanager/CallManager.kt
index 4f7f2f4..c71cf53 100644
--- a/app/src/main/java/com/example/autocallmanager/CallManager.kt
+++ b/app/src/main/java/com/example/autocallmanager/CallManager.kt
@@ -137,9 +137,19 @@ object CallManager {
     ): CallResult {
         val normalized = number.trim()
         if (normalized.isBlank()) {
+            ExecutionTraceStore.log(
+                context, taskId, executionId, attempt,
+                ExecutionTraceStore.Stage.PLACE_CALL, ExecutionTraceStore.FAIL,
+                "empty phone number"
+            )
             return CallResult(false, "INVALID_NUMBER", "Phone number is empty")
         }
         if (!isValidPhoneNumber(normalized)) {
+            ExecutionTraceStore.log(
+                context, taskId, executionId, attempt,
+                ExecutionTraceStore.Stage.PLACE_CALL, ExecutionTraceStore.FAIL,
+                "invalid phone number format"
+            )
             return CallResult(false, "INVALID_NUMBER", "Phone number format is invalid")
         }
         if (ContextCompat.checkSelfPermission(
@@ -147,12 +157,23 @@ object CallManager {
                 Manifest.permission.CALL_PHONE
             ) != PackageManager.PERMISSION_GRANTED
         ) {
+            ExecutionTraceStore.log(
+                context, taskId, executionId, attempt,
+                ExecutionTraceStore.Stage.PLACE_CALL, ExecutionTraceStore.FAIL,
+                "CALL_PHONE permission not granted"
+            )
             return CallResult(false, "CALL_PERMISSION_MISSING", "Call permission is required")
         }
 
         when (CallStateManager.current(context)) {
-            CallStateManager.State.RINGING, CallStateManager.State.OFFHOOK ->
+            CallStateManager.State.RINGING, CallStateManager.State.OFFHOOK -> {
+                ExecutionTraceStore.log(
+                    context, taskId, executionId, attempt,
+                    ExecutionTraceStore.Stage.PLACE_CALL, ExecutionTraceStore.FAIL,
+                    "device call state busy - another cellular call is already active"
+                )
                 return CallResult(false, "DEVICE_CALL_BUSY", "Another cellular call is already active")
+            }
             else -> Unit
         }
 
@@ -160,7 +181,14 @@ object CallManager {
         return try {
             val telecom = context.getSystemService(Context.TELECOM_SERVICE)
                 as? TelecomManager
-                ?: return CallResult(false, "TELECOM_UNAVAILABLE", "Telecom service is unavailable")
+                ?: run {
+                    ExecutionTraceStore.log(
+                        context, taskId, executionId, attempt,
+                        ExecutionTraceStore.Stage.PLACE_CALL, ExecutionTraceStore.FAIL,
+                        "TelecomManager service unavailable", phoneAccount = accountLabel
+                    )
+                    return CallResult(false, "TELECOM_UNAVAILABLE", "Telecom service is unavailable")
+                }
 
             val extras = android.os.Bundle()
             if (phoneAccountComponent.isNotBlank() && phoneAccountId.isNotBlank()) {
@@ -180,13 +208,32 @@ object CallManager {
                         Manifest.permission.READ_PHONE_STATE
                     ) != PackageManager.PERMISSION_GRANTED
                 ) {
+                    ExecutionTraceStore.log(
+                        context, taskId, executionId, attempt,
+                        ExecutionTraceStore.Stage.PLACE_CALL, ExecutionTraceStore.FAIL,
+                        "READ_PHONE_STATE permission not granted for SIM account resolution",
+                        phoneAccount = accountLabel
+                    )
                     return CallResult(false, "SIM_ACCOUNT_UNAVAILABLE", "Reading SIM account state requires phone-state permission")
                 }
                 val component = ComponentName.unflattenFromString(phoneAccountComponent)
-                    ?: return CallResult(false, "SIM_ACCOUNT_INVALID", "Selected SIM account is invalid")
+                    ?: run {
+                        ExecutionTraceStore.log(
+                            context, taskId, executionId, attempt,
+                            ExecutionTraceStore.Stage.PLACE_CALL, ExecutionTraceStore.FAIL,
+                            "selected SIM account identifier could not be parsed", phoneAccount = accountLabel
+                        )
+                        return CallResult(false, "SIM_ACCOUNT_INVALID", "Selected SIM account is invalid")
+                    }
                 val handle = PhoneAccountHandle(component, phoneAccountId)
 
                 if (!telecom.callCapablePhoneAccounts.contains(handle)) {
+                    ExecutionTraceStore.log(
+                        context, taskId, executionId, attempt,
+                        ExecutionTraceStore.Stage.PLACE_CALL, ExecutionTraceStore.FAIL,
+                        "selected SIM account is no longer in the call-capable account list",
+                        phoneAccount = accountLabel
+                    )
                     return CallResult(false, "SIM_ACCOUNT_UNAVAILABLE", "Selected SIM is no longer available")
                 }
 
@@ -195,6 +242,11 @@ object CallManager {
                 }.getOrDefault(true)
 
                 if (!permitted) {
+                    ExecutionTraceStore.log(
+                        context, taskId, executionId, attempt,
+                        ExecutionTraceStore.Stage.PLACE_CALL, ExecutionTraceStore.FAIL,
+                        "Telecom.isOutgoingCallPermitted() returned false", phoneAccount = accountLabel
+                    )
                     return CallResult(false, "CALL_NOT_PERMITTED", "Telecom did not permit this call")
                 }
 
diff --git a/app/src/main/java/com/example/autocallmanager/CallReceiver.kt b/app/src/main/java/com/example/autocallmanager/CallReceiver.kt
index d7114a0..023a789 100644
--- a/app/src/main/java/com/example/autocallmanager/CallReceiver.kt
+++ b/app/src/main/java/com/example/autocallmanager/CallReceiver.kt
@@ -20,7 +20,14 @@ import androidx.core.content.ContextCompat
 class CallReceiver : BroadcastReceiver() {
     override fun onReceive(context: Context, intent: Intent) {
         val taskId = intent.getLongExtra(CallScheduler.EXTRA_TASK_ID, -1L)
-        if (taskId <= 0L) return
+        if (taskId <= 0L) {
+            ExecutionTraceStore.log(
+                context, taskId, "", 0,
+                ExecutionTraceStore.Stage.RECEIVER_STARTED, ExecutionTraceStore.FAIL,
+                "invalid/missing EXTRA_TASK_ID (received $taskId) action=${intent.action}"
+            )
+            return
+        }
 
         val isCallAttempt = intent.action == CallScheduler.ACTION_CALL_ATTEMPT
         val attempt = if (isCallAttempt) {
@@ -73,7 +80,14 @@ class CallReceiver : BroadcastReceiver() {
 
     private fun handleCallAttempt(context: Context, taskId: Long, executionId: String, attempt: Int) {
         val tasks = TaskStore.load(context)
-        val task = tasks.firstOrNull { it.id == taskId } ?: return
+        val task = tasks.firstOrNull { it.id == taskId } ?: run {
+            ExecutionTraceStore.log(
+                context, taskId, executionId, attempt,
+                ExecutionTraceStore.Stage.CALL_EXECUTION_BEGIN, ExecutionTraceStore.FAIL,
+                "no task found for id=$taskId"
+            )
+            return
+        }
 
         if (SettingsStore.pausedAll(context) || task.status != "Scheduled") {
             ExecutionTraceStore.log(
